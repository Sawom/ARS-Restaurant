import React from 'react';
import not from '../../assets/not.gif';

const NotFound = () => {
    return (
        <div>
            <img className='w-full' src={not} alt="" />
        </div>
    );
};

export default NotFound;